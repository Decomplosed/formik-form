import { Card, CardContent, Typography } from '@material-ui/core';

export default function Home() {
  return (
    <Card>
      <CardContent>
        <Formik>
          <Form></Form>
        </Formik>
      </CardContent>
    </Card>
  );
}
